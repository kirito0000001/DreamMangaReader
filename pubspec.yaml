name: dream_manga_reader
description: "跨平台漫画阅读器 (Android + Windows)"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.14.0+25

environment:
  sdk: ^3.6.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  # 多语言:Material/Cupertino 组件自身的本地化(日期选择、文本选择菜单、默认语义等)。
  flutter_localizations:
    sdk: flutter
  # gen-l10n 生成的 AppLocalizations 依赖 intl(版本跟随 flutter SDK 约束)。
  intl: 0.20.2

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_inappwebview: ^6.1.5
  dio: ^5.10.0
  flutter_js: ^0.8.7
  html: ^0.15.6
  path_provider: ^2.1.6
  lzstring: ^2.0.0+2
  crypto: ^3.0.7
  # @visibleForTesting 等注解(核心层是纯 Dart,不引 flutter/foundation)。
  meta: ^1.18.0
  encrypt: ^5.0.3
  archive: ^3.6.1 # zip 导入源:解压 index.json + 多脚本
  charset: ^2.0.1
  # 精确锁定而非 ^:上游已到 2.5.1,但这是带原生实现的插件,
  # 放开前需要在 Android 真机上回归一遍 TXT 编码探测。
  charset_converter: 2.3.0
  # epubx 4 锁定 image 3,与图标工具的 image 4 冲突;epub_plus 是兼容 API 的 Dart 3 维护分支。
  epub_plus: ^5.1.0
  # 小说阅读器自有纸张纹理生成及用户背景图片解码/规范化。
  image: 4.3.0
  shared_preferences: ^2.5.5
  # IAM 账号登录后 access/refresh token 的安全存储(Android Keystore / Windows DPAPI)。
  flutter_secure_storage: ^9.2.4
  cached_network_image: ^3.4.1
  scrollable_positioned_list: ^0.3.8
  flutter_cache_manager: ^3.4.1
  url_launcher: ^6.3.1
  flutter_staggered_grid_view: ^0.7.0
  file_picker: ^11.0.2
  # 保存/分享当前页:移动端走系统分享面板(含保存到相册/文件),桌面走 file_picker 另存为。
  share_plus: ^10.0.0
  # 应用内更新:下载新版 APK 后用系统安装器打开(Android)。桌面自更新走自写的静默安装器脚本。
  open_filex: ^4.5.0
  # 桌面(Windows)枚举系统字体:GDI EnumFontFamiliesEx,无子进程/无控制台闪窗。
  # 仅在 Platform.isWindows 时调用;win32 的 DLL 句柄惰性加载,Android 不受影响。
  win32: ^5.5.3
  ffi: ^2.1.3
  # 阅读/播放时保持屏幕常亮。media_kit 已间接依赖,这里提升为直接依赖以便阅读器调用。
  wakelock_plus: ^1.2.8
  # 小说阅读器状态层显示真实电量;支持 Android 与 Windows。
  battery_plus: ^7.1.1
  # 番剧播放:media_kit(libmpv 后端),Win + Android 都支持 HLS/m3u8、硬解、
  # 自定义 HTTP 头(番剧 CDN 锁 Referer)。libs_video 按平台拉原生库(Windows
  # 构建期下载 libmpv)。
  media_kit: ^1.1.11
  media_kit_video: ^1.2.5
  media_kit_libs_video: ^1.0.5
  # HLS 清单采用结构化解析/重组，供本地鉴权缓存网关改写 URI。
  hls: 1.0.0
  # 播放页左半屏上下滑调亮度。media_kit_video 本来就依赖它(自带控件的亮度手势
  # 用的同一套),这里提为直接依赖只为自己调用 —— 不额外引入原生实现。
  # 用的是 application 级亮度:只影响本 app 且退出即还原,不去改系统设置。
  # 原生实现只有 Android(screen_brightness_android),桌面上调用会抛,已 catch。
  screen_brightness_platform_interface: ^2.1.2
  # B站登录二维码渲染(扫码登录)。
  qr_flutter: ^4.1.0
  # 小说仿真翻页:复用 MIT 的触点圆柱/贝塞尔卷页几何；精确锁定内部曲线 API。
  page_curl_effect: 1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  fake_async: 1.3.3
  file: 7.0.1

  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  # 从 assets/logo.png 生成各平台应用图标:`dart run flutter_launcher_icons`。
  flutter_launcher_icons: ^0.14.4
  xml: ^6.6.1

# 应用图标配置(源图 assets/logo.png,漫画风「梦」印)。
flutter_launcher_icons:
  image_path: "assets/logo.png"
  android: true
  windows:
    generate: true
    image_path: "assets/logo.png"
    icon_size: 256

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # 多语言:开启 flutter gen-l10n 代码生成(读 l10n.yaml + lib/l10n/app_*.arb)。
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/data/t2s.txt
    - assets/fonts/

  shaders:
    - assets/shaders/novel_page_curl.frag

  fonts:
    - family: DMRNotoSerifSC
      fonts:
        - asset: assets/fonts/NotoSerifSC-Regular.otf
    - family: DMRLXGWWenKai
      fonts:
        - asset: assets/fonts/LXGWWenKai-Regular.ttf

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
