# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/android/build/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Test fixtures (scraped third-party HTML, not committed)
# 例外:hls/ 下是手写的清单,解析器测试要读它们 —— 以前是 -f 强塞进来的,
# 于是下一个人新加一份 fixture 就会在本地全绿、到 CI 上因为文件不存在而挂。
# 目录一旦被排除,里面的文件就再也 ! 不回来,所以排的是 * 而不是目录本身。
test/fixtures/*
!test/fixtures/hls/

# 本地漫画源(引擎不内置源;运行时从这里/远端加载 index.json + 脚本)。不进公开仓库。
sources_local/

# Windows 打包产物(zip / setup.exe)
/dist/
/ReleaseOutput/
/Scripts/*.response.json
/.tools/

# flutter gen-l10n 生成物(源在 lib/l10n/*.arb;pub get 时自动重新生成)
/lib/l10n/app_localizations*.dart
